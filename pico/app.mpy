import network
from machine import Pin
from time import sleep
import urequests as requests
print('init success')
def connect():
    wlan = network.WLAN(network.STA_IF)
    wlan.active(True)
    wlan.connect('yun10122',"yunben5254")
    while wlan.isconnected() == False:
        print('Waiting for connection...')
        sleep(1)
connect()


